[project]
name = "uv-update-check"
version = "0.3.0"
description = "Check for dependency updates in pyproject.toml, like ncu for uv projects"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "tomlkit>=0.15.0",
    "packaging>=26.2",
    "httpx>=0.28.1",
    "rich>=15.0.0",
    "typer>=0.26.8",
    "anyio>=4.14.1",
]

[project.scripts]
uuc = "uv_update_check.cli:app"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "respx>=0.23.1",
    "ruff>=0.15.20",
    "pytest-cov>=7.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/uv_update_check"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "--cov=uv_update_check --cov-report=term-missing"

[tool.coverage.run]
source = ["uv_update_check"]

[tool.coverage.report]
show_missing = true
skip_empty = true
exclude_lines = [
    "if __name__ == .__main__.",
    "pragma: no cover",
]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
]
ignore = [
    "SIM108", # ternary operator - sometimes less readable
    "RUF",   # ruff-specific rules
]

[tool.ruff.format]
quote-style = "double"
